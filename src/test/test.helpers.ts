import { NAME } from '../index'

export const openName = `${NAME}_open`
export const closeName = `${NAME}_close`
export const metadataOpenName = `${NAME}_metadata_open`
export const metadataCloseName = `${NAME}_metadata_close`
